import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>Страницы с текущим адресом не существует</h1>
        </div>
    );
};

export default Error;